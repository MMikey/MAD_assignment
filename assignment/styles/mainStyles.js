import { StyleSheet } from 'react-native'

const mainStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignContent: 'center'
  },

  titleText: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'blue'
  },

  buttonLink: {
    color: 'red'
  }
})

export { mainStyles }
